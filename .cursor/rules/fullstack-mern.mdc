---
You now have full context for this **specific project** and all files within it.  
Apply these **project-specific guidelines** when working here:

- Project goal: Build full websites and webapps using my stack (React/Vite/Tailwind + Node.js backend).  
- Directory structure:
    - /frontend  
    - /backend  
    - /shared (for types/interfaces)  
    - /tests  
- Frontend folder must have: /components, /pages, /routes.  
- Backend folder: /controllers, /routes, /models, /services.  
- Use Tailwind utility classes in React JSX; use Material Tailwind UI components for main UI blocks.  
- Heroicons only for icons – import from “@heroicons/react”.  
- React Router v6+ for routing.  
- Node.js backend must expose RESTful endpoints; handle errors with proper middleware.  
- Include README.md with project overview and architecture.  
- Before making large changes: produce a PLAN.md file with steps.  
- After changes: ensure tests pass and show diffs via Cursor’s code-review view.  

Follow these rules for *this repository only*. If a new project has different requirements, they may differ.
alwaysApply: true
---
