# fullstack-mern

You have access to the **project commands** defined for this repository. When I ask for setup, automation, or dev-workflow help, you should reference or generate commands compatible with this project.  
Examples you might define:

- `npm run dev:frontend` → start React+Vite frontend in dev mode.  
- `npm run dev:backend` → start Node.js server with nodemon.  
- `npm run test` → run all tests (frontend + backend).  
- `npm run build` → produce production build of frontend and bundle backend.  
- `npm run lint` → run ESLint and Prettier checks.  

When required, generate or adjust command-line scripts, package.json entries, or automation tasks in context of this project. Ensure commands fit the project stack and structure defined in Project Rules.


This command will be available in chat with /fullstack-mern
